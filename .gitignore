# Source documents used to write the site — not published.
# GitHub Pages serves everything committed to this repo, so keep CV/biosketch
# drafts out of git until they are reviewed and intentionally published.
*.docx
*.doc
*.pdf
*Biosketch*
*bio_nih*
*CV*

# To publish one of these deliberately, force it: git add -f <file>

*.review.md
